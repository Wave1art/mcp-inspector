[project]
name = "mcp-inspector-test-server"
version = "0.1.0"
description = "Mock MCP server for testing the MCP Dev Inspector"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
]

[project.scripts]
test-server = "server:main"
